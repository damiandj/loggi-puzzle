# loggi-puzzle

## Usage
``` 
python loggi_puzzle/main.py -h
usage: main.py [-h] [-ip IMAGE_PATH] [-size-x PUZZLE_SIZE_X]
               [-size-y PUZZLE_SIZE_Y]

Create puzzle.

optional arguments:
  -h, --help            show this help message and exit
  -ip IMAGE_PATH, --image-path IMAGE_PATH
                        image path
  -size-x PUZZLE_SIZE_X, --puzzle-size-x PUZZLE_SIZE_X
                        Puzzle size x
  -size-y PUZZLE_SIZE_Y, --puzzle-size-y PUZZLE_SIZE_Y
                        Puzzle size yusage: main.py [-h] -ip IMAGE_PATH -size-x PUZZLE_SIZE_X -size-y PUZZLE_SIZE_Y
               [-sp SAVE_PATH]

Create puzzle.

optional arguments:
  -h, --help            show this help message and exit
  -ip IMAGE_PATH, --image-path IMAGE_PATH
                        image path
  -size-x PUZZLE_SIZE_X, --puzzle-size-x PUZZLE_SIZE_X
                        Puzzle size x
  -size-y PUZZLE_SIZE_Y, --puzzle-size-y PUZZLE_SIZE_Y
                        Puzzle size y
  -sp SAVE_PATH, --save-path SAVE_PATH
                        Output save path

python loggi_puzzle/main.py -ip apple.jpg -size-x 25 -size-y 25
```
